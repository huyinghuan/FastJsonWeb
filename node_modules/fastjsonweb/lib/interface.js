var server = require("./server");
var routerHandle;
exports.createServer = function(){
	if(!routerHandle){
		console.log("routerHandle 是空");
	}else{
		console.log(routerHandle.handle);
	}
	return server.createServer;
}
exports.setRouterHandle = function(handle){
	routerHandle = handle;
}